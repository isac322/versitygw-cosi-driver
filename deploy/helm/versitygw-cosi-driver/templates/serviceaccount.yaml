{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "versitygw-cosi-driver.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "versitygw-cosi-driver.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
